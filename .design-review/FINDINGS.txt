ARCADE MAPS - DESIGN REVIEW FINDINGS (scratch notes; report lives in chat)
Screenshots: C:/Users/jonny/Desktop/Arcade Maps/.design-review/  (UNTRACKED, inside repo - gitignore or delete)
Server :8662, Playwright isolated contexts, console 0 msgs on every load/interaction.

RETRACTED (false positives from calling AM.panel.openPlace(a,{}) directly).
MECHANISM (proven, not guessed): every dismissal control routes through
AM.state.set("selectedArcade", null, ...) - panel.js:717 (X), :544 (back),
:755 (Esc). Every real opener sets that state first: :794 marker, :828 hash,
search.js:530, nearby.js:237. Calling openPlace() directly renders the panel
WITHOUT setting state, so selectedArcade is still null and setting it to null
is a no-op - no change event, no close. Proof: MutationObserver on #place
logged ZERO attribute mutations after the close click, while
AM.panel.closePlace() closed it instantly. flyTo was NOT the cause: dismissal
works both mid-flight (400ms) and after settle (41, 42).
  - "Nearby list invisible on desktop"  -> works on real marker click (36)
  - "Back/X/Esc cannot dismiss panel"   -> all work on real paths (35, 41, 42)

P1
1. Legend teaches 3 different taxonomies of ONE encoding.
   - map draws 5 size classes + unknown (markers.js:106-115)
   - collapsed chip legend shows 4 samples: 1-2, 7-12, 25+, ? (03)
   - Settings > About shows 6 rows (09-settings-about)
   Worse: UNKNOWN_CLASS r=6 == M class r=6, so unknown and 3-6 cabs are
   pixel-identical on the map; the chip's "?" swatch is 12px, sitting between
   the 8px (1-2) and 17px (7-12) samples, so it reads as the missing 3-6 class.
2. Gold cluster ring is an unexplained visual channel. clusterHasBig ->
   ring = "contains an XL/XXL store" (markers.js:39-49, 79-83). In neither legend.
3. Action-row icon stagger. #place button{padding:0} (style.css:476) overrides
   .act{padding:6px 2px 8px} (572-575) - later rule, equal specificity. <a>
   Directions/ZIv icons sit 6px lower than <button> Nearby/Share. (06)
4. Nearby top-2 rows are the store you are already looking at: origin at
   "0.0 m" plus its duplicate 13 m away (ZIv romaji vs JP entry of same venue). (36)
5. Game count mismatch: chip "SOUND VOLTEX 1,097" (all rows) vs omnibox game row
   "1,087" (plottable only). Same label, two numbers.
6. Deep-link hash hygiene: X close rewrites hash and drops arcade=8472; Back
   leaves arcade=8472 stale, so reload re-opens a dismissed panel. (38 vs 39)
7. Mobile default view mostly empty: z0.25, world drawn 304px tall in a 766px
   container -> 245px empty above / 217px below, plus a 78.7px static footer
   eating viewport. First thing every phone visitor sees. (15)
8. Settings modal jumps vertically per section (whole dialog incl. nav):
   top = Sources 141.9, Display 339, Location 328.8, About 54. left constant 360.

P2
9.  XXL legend swatch (28px) leaves 1px to its label; other rows 11px. (10)
10. Empty state (chips "none") = "0 shown" only; no on-map guidance. (40)
11. Zero-result omnibox is a bare "No matches" row. (12)
12. Mobile tap targets under 44px: #pl-close 18x32, #pl-back 59x32.
13. Mobile nearby: drawer 326 of 390px leaves a raw map strip; "Search this
    area" pill overlaps the tab bar. (29)
14. Game chips wrap to a ragged 1-3 per row grid. (21)

GOOD (do not regress): settings modal IA + switches, and its mobile form (left
nav becomes horizontal tab strip, fits 370x774) (17); Esc closes + restores
focus to #settings-btn; focus rings consistent 2px accent across all 9 tabbed
elements; chip on/off unmistakable (21); denied-geolocation toast copy (22);
About legend thoroughness; omnibox grouping + match highlighting (11); sparse
panel omits empty rows without collapsing (07); topbar holds to 768px, no
collision; sheet drag 38%->74% (30); mobile hero collapse to 6px accent bar is
deliberate (style.css:706-714).

Real game_counts are live: 6,271 of 13,872 rows; Tokyo 5 XXL / 18 XL, max 37;
global max 55 (Round1 Kyoto Kawaramachi). No synthetic injection needed or used.
